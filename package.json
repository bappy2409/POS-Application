{
  "name": "clothing-store-pos",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "dev": "pnpm --parallel --filter server --filter pos-app dev",
    "dev:server": "pnpm --filter server dev",
    "dev:pos": "pnpm --filter pos-app dev",
    "build": "pnpm -r build"
  }
}
